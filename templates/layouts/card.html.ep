% my $style = stash('hidden') ? 'style="display:none"' : '';

<div class="row justify-content-sm-center mx-sm-1 my-1">
  <div class="col-sm-auto">
    <div id="<%= $id %>" <%== $style %>
         class="flashcard card text-white mb-2">

        <%= content %>

    </div>
  </div>
</div>
